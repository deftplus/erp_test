
#Область ПрограммныйИнтерфейс

// Возвращает имя, по которому программа идентифицируется в
// сервисах Интернет-поддержки.
//
// Возвращаемое значение:
//	Строка - имя программы. <Пустая строка>, если имя программы не указано.
//
Функция ИмяПрограммыДляИдентификацииВСервисахИнтернетПоддержки() Экспорт
	
	ИмяПрограммы = "";
	Если ВРег(Метаданные.Имя) = ВРег("УправлениеХолдингом") Тогда
		ИмяПрограммы = "CPM";
	ИначеЕсли ВРег(Метаданные.Имя) = ВРег("БухгалтерияКорпМСФО") Тогда
		ИмяПрограммы = "AccountingCorpIFRS";
	ИначеЕсли ВРег(Метаданные.Имя) = ВРег("НалоговыйМониторинг") Тогда
		ИмяПрограммы = "TaxMonitoring";
	ИначеЕсли ВРег(Метаданные.Имя) = ВРег("УправлениеХолдингомERP") Тогда
		ИмяПрограммы = "ERP_UH";
	КонецЕсли;
	
	Возврат ИмяПрограммы;
	
КонецФункции

#КонецОбласти
