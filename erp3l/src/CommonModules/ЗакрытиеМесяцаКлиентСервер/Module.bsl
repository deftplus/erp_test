#Область ПрограммныйИнтерфейс

// Возвращает имя события журнала регистрации функционального блока "Закрытие месяца"
//
// Параметры:
//  ПредставлениеВидаОперации	 - Строка - синоним вида регл. операции
// 
// Возвращаемое значение:
//  Строка - имя события журнала регистрации
//
Функция СобытиеЖурналаРегистрации(ПредставлениеВидаОперации = "") Экспорт
	
	ИмяГруппы = НСтр("ru = 'Закрытие месяца';
					|en = 'Month-end closing'", ОбщегоНазначенияКлиентСервер.КодОсновногоЯзыка()); // Строка записывается в журнал
	
	Если ПустаяСтрока(ПредставлениеВидаОперации) Тогда
		Возврат ИмяГруппы;
	КонецЕсли;
	
	Возврат ИмяГруппы + "." + ПредставлениеВидаОперации;
	
КонецФункции

// Функция - Отчет использует механизм актуализации
//
// Параметры:
//  Форма	 - ФормаКлиентскогоПриложения
// 
// Возвращаемое значение:
//  Булево - истина, если использует
//
Функция ОтчетИспользуетМеханизмАктуализации(Форма) Экспорт
	
	Возврат Форма.Элементы.Найти("Актуализация") <> Неопределено;
	
КонецФункции

// Вызывается в контексте формы.
//  Предварительно требует проверки ОтчетИспользуетМеханизмАктуализации()
//
// Параметры:
//  Форма	 - ФормаКлиентскогоПриложения
//
Процедура СкрытьПанельАктуализации(Форма) Экспорт
	
	Форма.Элементы.Актуализация.Видимость = Ложь;
	
КонецПроцедуры

#КонецОбласти