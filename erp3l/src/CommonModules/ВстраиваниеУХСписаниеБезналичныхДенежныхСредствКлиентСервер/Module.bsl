#Область ПрограммныйИнтерфейс
Функция МассивШаблоновИменРеквизитовВал()
	
	МассивИмен = Новый Массив;
	МассивИмен.Добавить("Плательщика");
	МассивИмен.Добавить("Получателя");
	МассивИмен.Добавить("БанкаПлательщика");
	МассивИмен.Добавить("БанкаПолучателя");
	МассивИмен.Добавить("БанкаПосредника");
	
	Возврат МассивИмен;
	
КонецФункции

Процедура ОбновитьВсеПечатныеРеквизитыВал(Форма) Экспорт
	
	МассивШаблонов = МассивШаблоновИменРеквизитовВал();
	Для каждого Шаблон Из МассивШаблонов Цикл
		ОбновитьПечатныеРеквизитыВал(Форма, Шаблон);
	КонецЦикла;
	
КонецПроцедуры 	
	
Процедура ОбновитьПечатныеРеквизитыВал(Форма, СтрокаЗамены) Экспорт
	
	Объект = Форма.Объект;
	ИмяРеквизитаФормы			= РеквизитФормыПечатныеРеквизитыВал(СтрокаЗамены);
	ИмяРеквизитаАдрес 			= РеквизитАдресВал(СтрокаЗамены);
	ИмяРеквизитаГородСтрана		= РеквизитГородСтранаВал(СтрокаЗамены);
	ИмяРеквизитаНаименование	= РеквизитНаименованиеВал(СтрокаЗамены);
	
	Форма[ИмяРеквизитаФормы] = СтрШаблон(НСтр("ru = 'Наименование: %1; Город, страна: %2, Адрес: %3'"),
		?(ПустаяСтрока(Объект[ИмяРеквизитаНаименование]), НСтр("ru = '<не задано>'"), Объект[ИмяРеквизитаНаименование]),
		?(ПустаяСтрока(Объект[ИмяРеквизитаГородСтрана]), НСтр("ru = '<не заданы>'"), Объект[ИмяРеквизитаГородСтрана]),
		?(ПустаяСтрока(Объект[ИмяРеквизитаАдрес]), НСтр("ru = '<не задан>'"), Объект[ИмяРеквизитаАдрес]));
		
КонецПроцедуры

Функция РеквизитФормыПечатныеРеквизитыВал(СтрокаЗамены)
	Возврат СтрШаблон("Реквизиты%1Вал", СтрокаЗамены);
КонецФункции

Функция РеквизитНаименованиеВал(СтрокаЗамены) Экспорт
	Возврат СтрШаблон("Наименование%1Вал", СтрокаЗамены);
КонецФункции

Функция РеквизитАдресВал(СтрокаЗамены) Экспорт
	Возврат СтрШаблон("Адрес%1Вал", СтрокаЗамены);
КонецФункции

Функция РеквизитГородСтранаВал(СтрокаЗамены) Экспорт
	Возврат СтрШаблон("ГородСтрана%1Вал", СтрокаЗамены);
КонецФункции
#КонецОбласти