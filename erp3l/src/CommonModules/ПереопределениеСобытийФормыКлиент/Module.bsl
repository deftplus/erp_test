#Область ПрограммныйИнтерфейс
	
Функция ПолучитьПредыдущиеОбработчики(Форма, ИмяЭлемента, ИмяСобытия) экспорт
	
	Попытка
	
		ТаблицаСобытий = Форма.ПереопределенныеСобытияЭлементов;
	
	Исключение
		Возврат Новый Массив;
	КонецПопытки; 
	
	СтруктураПоиска = Новый Структура("ИмяЭлемента, ИмяСобытия", ИмяЭлемента, ИмяСобытия);
	
	Возврат ТаблицаСобытий.НайтиСтроки(СтруктураПоиска);
	
КонецФункции
	
#КонецОбласти

