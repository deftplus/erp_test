#Область СлужебныйПрограммныйИнтерфейс

Процедура ПриИзмененииПоляФормы(Форма, Элемент) Экспорт
	Если ИдентификацияПродуктаУХКлиентСервер.ЭтоУправлениеХолдингом() Тогда
		Возврат;
	КонецЕсли;
	
	ИмяФормы = Форма.ИмяФормы;
	
	ОбщийМодуль = Неопределено;
	
	Если ИмяФормы = "Справочник.ДоговорыКонтрагентов.Форма.ФормаЭлемента" Тогда
		ОбщийМодуль = ДоговорыКонтрагентовФормыБМКлиент;
	ИначеЕсли ИмяФормы = "Справочник.ЦенныеБумаги.Форма.ФормаЭлемента" Тогда
		ОбщийМодуль = ЦенныеБумагиФормыБМКлиент;
	КонецЕсли;
	
	Если ОбщийМодуль <> Неопределено Тогда
		ОбщийМодуль.ПриИзмененииПоляФормы(Форма, Элемент);
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти
