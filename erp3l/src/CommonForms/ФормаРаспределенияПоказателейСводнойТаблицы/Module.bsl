
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ПоказательРаспределения = Параметры.ПоказательРаспределения;
	ЗначениеКРаспределению =  Параметры.ЗначениеКРаспределению;
    ИсходныйСценарий = 	      Параметры.ИсходныйСценарий;
	
КонецПроцедуры


&НаКлиенте
Процедура Применить(Команда)
	
	СтруктураПараметров = Новый Структура;
	СтруктураПараметров.Вставить("СценарийРаспределения",СценарийРаспределения);
	СтруктураПараметров.Вставить("ПоказательРаспределения",ПоказательРаспределения);
	СтруктураПараметров.Вставить("ЗначениеКРаспределению",ЗначениеКРаспределению);
    СтруктураПараметров.Вставить("ИсходныйСценарий",ИсходныйСценарий);
	
	Оповестить("СТ_ИзмененыПоказателиРаспределения",СтруктураПараметров);
	Закрыть();
		
КонецПроцедуры
