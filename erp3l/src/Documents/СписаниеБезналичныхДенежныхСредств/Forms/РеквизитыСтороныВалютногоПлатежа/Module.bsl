
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ЗаполнитьЗначенияСвойств(ЭтотОбъект, Параметры,"Адрес,Наименование,ГородСтрана");
	
	Если Параметры.ВидСтороны = "Плательщика" Тогда
		Заголовок = НСтр("ru = 'Реквизиты плательщика'");
	ИначеЕсли Параметры.ВидСтороны = "Получателя" Тогда
		Заголовок = НСтр("ru = 'Реквизиты получателя'");
	ИначеЕсли Параметры.ВидСтороны = "БанкаПлательщика" Тогда
		Заголовок = НСтр("ru = 'Реквизиты банка плательщика'");
	ИначеЕсли Параметры.ВидСтороны = "БанкаПолучателя" Тогда
		Заголовок = НСтр("ru = 'Реквизиты банка получателя'");
	ИначеЕсли Параметры.ВидСтороны = "БанкаПосредника" Тогда
		Заголовок = НСтр("ru = 'Реквизиты банка-посредника'");
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ОК(Команда)
	Результат = Новый Структура("Наименование,Адрес,ГородСтрана");
	ЗаполнитьЗначенияСвойств(Результат, ЭтотОбъект);
	Закрыть(Результат);
КонецПроцедуры
