
////////////////////////////////////////////////////////////////////////////////

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если (Источник <> ЭтаФорма) И (ИмяСобытия="ЗаписанОбъектТрансформационнаяКорректировка") Тогда
		
		Элементы.Список.Обновить();
		
	КонецЕсли;
	
КонецПроцедуры
