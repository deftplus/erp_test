#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
// Возвращает остаток затрат на устранение рискового события РисковоеСобытиеВход.
Функция ПолучитьОстатокЗатратНаУстранениеРисковогоСобытия(РисковоеСобытиеВход) Экспорт
	РезультатФункции = 0;
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	ЗатратыНаЛиквидациюРисковОстатки.РисковоеСобытие КАК РисковоеСобытие,
		|	СУММА(ЗатратыНаЛиквидациюРисковОстатки.СуммаОстаток) КАК СуммаОстаток
		|ИЗ
		|	РегистрНакопления.ЗатратыНаЛиквидациюРисков.Остатки(, РисковоеСобытие = &РисковоеСобытие) КАК ЗатратыНаЛиквидациюРисковОстатки
		|
		|СГРУППИРОВАТЬ ПО
		|	ЗатратыНаЛиквидациюРисковОстатки.РисковоеСобытие";
	Запрос.УстановитьПараметр("РисковоеСобытие", РисковоеСобытиеВход);
	РезультатЗапроса = Запрос.Выполнить();
	Выгрузка = РезультатЗапроса.Выгрузить();
	РезультатФункции = Выгрузка.Итог("СуммаОстаток");
	Возврат РезультатФункции;
КонецФункции		// ПолучитьОстатокЗатратНаУстранениеРисковогоСобытия()

#КонецЕсли