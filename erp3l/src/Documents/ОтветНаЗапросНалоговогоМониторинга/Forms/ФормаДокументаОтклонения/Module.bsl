
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Объект.Ссылка.Пустая() Тогда
		Объект.ЗапросОтклонен = Истина;
	КонецЕсли;
	Элементы.Дата.ТолькоПросмотр = РольДоступна("НалоговыйМониторинг");
КонецПроцедуры
