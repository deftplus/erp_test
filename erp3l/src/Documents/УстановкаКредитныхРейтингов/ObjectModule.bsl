#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Процедура ОбработкаПроведения(Отказ, Режим)
	
	Движения.КредитныеРейтингиКонтрагентов.Записывать = Истина;
	Для Каждого ТекСтрокаРейтинг Из Рейтинг Цикл
		Движение = Движения.КредитныеРейтингиКонтрагентов.Добавить();
		Движение.Период = Дата;
		Движение.Контрагент = ТекСтрокаРейтинг.Контрагент;
		Движение.ШкалаКредитногоРейтинга = ШкалаКредитногоРейтинга;
		Движение.КредитныйРейтинг = ТекСтрокаРейтинг.КредитныйРейтинг;
		Движение.Прогноз = ТекСтрокаРейтинг.Прогноз;
	КонецЦикла;

КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Дата = ТекущаяДатаСеанса();
	Ответственный = Пользователи.ТекущийПользователь();
	
КонецПроцедуры

#КонецЕсли