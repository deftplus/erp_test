
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Документ = ПараметрКоманды;
	
	ПараметрыФормы = Новый Структура("Отбор,СформироватьПриОткрытии", 
										ПолучитьОтбор(Документ), 
										Истина										
									);
	
	ОткрытьФорму("Отчет.СверкаВГО.Форма", 
						ПараметрыФормы, 
						ПараметрыВыполненияКоманды.Источник, 
						ПараметрыВыполненияКоманды.Уникальность, 
						ПараметрыВыполненияКоманды.Окно, 
						ПараметрыВыполненияКоманды.НавигационнаяСсылка
				);
	
КонецПроцедуры

&НаСервере
Функция ПолучитьОтбор(Знач Документ)

	ПериодОтчета = Новый СтандартныйПериод(Документ.ПериодОтчета.ДатаНачала, Документ.ПериодОтчета.ДатаОкончания);
	
	Организация = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Документ.ОрганизацияА);
	Организация.Добавить(Документ.ОрганизацияБ);
		
	Результат = Новый Структура;	
	Результат.Вставить("Сценарий", 				Документ.Сценарий);
	Результат.Вставить("Период", 				ПериодОтчета);	
	Результат.Вставить("ПланСчетов", 			Справочники.ПланыСчетовБД.ПолучитьПланСчетовБД("МСФО"));
		
	Возврат Результат;

КонецФункции
