
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Лот = Параметры.Лот;
	ЗакупочнаяПроцедура = Лот.Владелец;
	ДатаСреза = Параметры.ДатаСреза;
	Если НЕ ЗначениеЗаполнено(ДатаСреза) Тогда
		ДатаСреза = ТекущаяДатаСеанса();
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(ЗакупочнаяПроцедура) Тогда
		Отказ = Истина;
		Сообщение = Новый СообщениеПользователю;
		Сообщение.Текст = НСтр(
			"ru='Форму выбора контрагентов квалифицированных по лоту можно открыть только с указанием закупочной процедуры!'");
		Сообщение.Сообщить();
	КонецЕсли;
	Список.Параметры.УстановитьЗначениеПараметра("ДатаСреза", ДатаСреза);
	Список.Параметры.УстановитьЗначениеПараметра(
		"ДопустимыеРазмеры", Перечисления.РазмерыКонтрагентовДляЕИС.ПолучитьРазмерыСМСП());
КонецПроцедуры
