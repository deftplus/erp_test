
Функция ПолучитьАвтоНаименование(Объект) Экспорт 

	Наименование = "";
	Если ЗначениеЗаполнено(Объект.КлассВНА) Тогда
		Наименование = Наименование + "Способ: " + Объект.СпособНачисленияАмортизации;
	КонецЕсли;
	
	Если Объект.СрокПолезногоИспользования <> 0 Тогда
		Наименование = ?(Наименование="", "", Наименование + ", ") + НСтр("ru = 'СПИ:'") + Объект.СрокПолезногоИспользования;
	КонецЕсли;
	
	Если НЕ Объект.СпособОтраженияРасходовПоАмортизации.Пустая() Тогда
		Наименование = ?(Наименование="", "", Наименование + ", ") + НСтр("ru = 'Профиль распределения:'") + Объект.СпособОтраженияРасходовПоАмортизации.Наименование;
	КонецЕсли;
	
	Возврат Наименование;

КонецФункции

Функция ПолучитьСобытияИзмененияПараметровАмортизации() Экспорт

	Мас = Новый Массив;
	
	Мас.Добавить(	ПредопределенноеЗначение("Справочник.ВидыОпераций.ВводВЭксплуатацию"));
	Мас.Добавить(	ПредопределенноеЗначение("Справочник.ВидыОпераций.ИзменениеПараметровАмортизации"));
	Мас.Добавить(	ПредопределенноеЗначение("Справочник.ВидыОпераций.МодернизацияВНА"));		
	//Мас.Добавить(	ПредопределенноеЗначение("Справочник.ВидыОпераций.Рекласс"));
	
	Возврат Мас;

КонецФункции
