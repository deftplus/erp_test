
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Отбор.Свойство("Владелец") Тогда
		
		Если ТипЗнч(Параметры.Отбор.Владелец)=Тип("СправочникСсылка.СправочникиБД") Тогда
			Элементы.Владелец.Заголовок="Справочник";
		ИначеЕсли ТипЗнч(Параметры.Отбор.Владелец)=Тип("СправочникСсылка.ДокументыБД") Тогда
			Элементы.Владелец.Заголовок="Документ";
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры
