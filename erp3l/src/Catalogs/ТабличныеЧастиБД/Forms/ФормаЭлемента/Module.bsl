
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ТолькоПросмотр = Истина;
	
	Если ТипЗнч(Объект.Владелец)=Тип("СправочникСсылка.СправочникиБД") Тогда
		Элементы.Владелец.Заголовок="Справочник";
	ИначеЕсли ТипЗнч(Объект.Владелец)=Тип("СправочникСсылка.ДокументыБД") Тогда
		Элементы.Владелец.Заголовок="Документ";
	КонецЕсли;
	
	Владелец=Объект.Владелец.Синоним;
	
КонецПроцедуры

&НаКлиенте
Процедура РеквизитыВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Если Поле.Имя="РеквизитыТипДанных" Тогда
		
		ДанныеОбъекта=Новый Структура;
		ДанныеОбъекта.Вставить("ИмяОбъекта",СтрШаблон(Нстр("ru = '%1 %2, табличная часть %3'"), НРег(Элементы.Владелец.Заголовок), Владелец, 
			Объект.Синоним));
		ДанныеОбъекта.Вставить("ТекстПоля",СтрШаблон(Нстр("ru = ' реквизит %1'"), Элементы.Реквизиты.ТекущиеДанные.Имя));
		ДанныеОбъекта.Вставить("ТипДанных",Элементы.Реквизиты.ТекущиеДанные.ТипДанных);	
		
		ОткрытьФорму("ОбщаяФорма.ФормаПросмотраТипаЗначений",ДанныеОбъекта);		
		
	КонецЕсли;
	
КонецПроцедуры
