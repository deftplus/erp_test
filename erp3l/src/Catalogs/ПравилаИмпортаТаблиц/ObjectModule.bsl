
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	Если ЭтоНовый() Тогда
		СсылкаНового = Справочники.ПравилаИмпортаТаблиц.ПолучитьСсылку();
		ЭтотОбъект.УстановитьСсылкуНового(СсылкаНового);
		УникальныйИдентификаторПравилаИмпорта = СсылкаНового.УникальныйИдентификатор();
	КонецЕсли;
	
	Если НЕ ЗначениеЗаполнено(УникальныйИдентификаторПравилаИмпорта) Тогда
		УникальныйИдентификаторПравилаИмпорта = Ссылка.УникальныйИдентификатор();
	КонецЕсли;
	
КонецПроцедуры
