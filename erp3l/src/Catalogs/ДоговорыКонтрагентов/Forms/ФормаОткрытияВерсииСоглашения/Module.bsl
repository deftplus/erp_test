#Область ОбработчикиСобытийФормы


&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	//ВходящиеПараметры = ОбщегоНазначенияКлиентСервер.СкопироватьРекурсивно(Параметры);
	ВходящиеПараметры = Новый Структура;
	
	КлючевыеСвойства = Новый Массив;
	КлючевыеСвойства.Добавить("ЗначениеКопирования");
	КлючевыеСвойства.Добавить("ЗначенияЗаполнения");
	КлючевыеСвойства.Добавить("Основание");
	КлючевыеСвойства.Добавить("ПараметрыВыбора");
	КлючевыеСвойства.Добавить("СозданИзФормыДокумента");
	КлючевыеСвойства.Добавить("ОткрытИзПлатежки");
	
	Для Каждого КлючевоеСвойство из КлючевыеСвойства Цикл
		Если Параметры.Свойство("КлючевоеСвойство") Тогда
			ВходящиеПараметры.Вставить(КлючевоеСвойство, Параметры[КлючевоеСвойство]);
		КонецЕсли;
	КонецЦикла;
	
	ВходящиеПараметры.Вставить("Ключ", Параметры.ВерсияСоглашения);
	ИмяДокумента = Параметры.ВерсияСоглашения.Метаданные().Имя;
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ИмяОткрываемойФормы = СтрШаблон("Документ.%1.ФормаОбъекта", ИмяДокумента);
	
	Модифицированность = Ложь;
	Закрыть();
	
	ОткрытьФорму(ИмяОткрываемойФормы, ВходящиеПараметры, ВладелецФормы);
	
КонецПроцедуры


#КонецОбласти
