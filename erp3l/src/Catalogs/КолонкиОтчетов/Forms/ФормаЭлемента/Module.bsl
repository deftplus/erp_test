
&НаКлиенте
Процедура КодПриИзменении(Элемент)
	
	Если ПустаяСтрока(Объект.Наименование) Тогда
		Объект.Наименование = УправлениеОтчетамиУХ.НаименованиеПоКоду(Объект.Код);
	КонецЕсли;

КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Ключ.Пустая() Тогда
		
		Объект.ПорядковыйНомер=Справочники.ВидыОтчетов.ПолучитьВозможныйНомерКолонки(Объект.Владелец);
		Объект.ТипЗначения=Перечисления.ТипыЗначенийПоказателейОтчетов.Число;
			
	КонецЕсли;
	
	УстановитьВидимость();		

КонецПроцедуры

&НаСервере
Процедура УстановитьВидимость()
	
	Если Объект.Предназначение=Перечисления.ПредназначенияЭлементовСтруктурыОтчета.ОборотноСальдоваяВедомость Тогда
		
		Элементы.ГруппаОстаткиОбороты.Видимость=Истина;
		Элементы.ГруппаОстаткиОбороты.ТолькоПросмотр=Истина;
		Элементы.ТипЗначения.ТолькоПросмотр=Истина;
		
		Элементы.ВидИтогаСокращеннойОСВ.Видимость	= Объект.Владелец.СокращеннаяОСВ;
		Элементы.ВидИтогаПоСчету.Видимость			= НЕ Объект.Владелец.СокращеннаяОСВ;
				
	Иначе
		
		Элементы.ГруппаОстаткиОбороты.Видимость=Ложь;
		
	КонецЕсли;
	
	Элементы.ГруппаЧисло.Видимость=(Объект.ТипЗначения=Перечисления.ТипыЗначенийПоказателейОтчетов.Число);
	Элементы.ГруппаОстаткиОбороты.Видимость=(Объект.ТипЗначения=Перечисления.ТипыЗначенийПоказателейОтчетов.Число);	
	
КонецПроцедуры // УстановитьВидимость() 

&НаКлиенте
Процедура ТипЗначенияПриИзменении(Элемент)
	
	УстановитьВидимость();
	
КонецПроцедуры
