Функция ПроверитьВхождениеВГруппу(Ссылка,ТекВидыОтчетов) Экспорт
	
	Запрос=Новый Запрос;
	Запрос.Текст="ВЫБРАТЬ
	|	ТекВидыОтчетов.ВидОтчета КАК ВидОтчета
	|ПОМЕСТИТЬ ТекВидыОтчетов
	|ИЗ
	|	&ТекВидыОтчетов КАК ТекВидыОтчетов
	|;
	|
	|////////////////////////////////////////////////////////////////////////////////
	|ВЫБРАТЬ
	|	ТекВидыОтчетов.ВидОтчета КАК ВидОтчета,
	|	ГруппыДоступаКВидамОтчетовВидыОтчетов.Ссылка КАК ГруппаДоступа
	|ИЗ
	|	ТекВидыОтчетов КАК ТекВидыОтчетов
	|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Справочник.ГруппыДоступаКВидамОтчетов.ВидыОтчетов КАК ГруппыДоступаКВидамОтчетовВидыОтчетов
	|		ПО ТекВидыОтчетов.ВидОтчета = ГруппыДоступаКВидамОтчетовВидыОтчетов.ВидОтчета
	|			И (НЕ ГруппыДоступаКВидамОтчетовВидыОтчетов.Ссылка = &Ссылка)"; 
	
	Запрос.УстановитьПараметр("ТекВидыОтчетов",ТекВидыОтчетов);
	Запрос.УстановитьПараметр("Ссылка",Ссылка);
	
	ТекстПроверки="";
	
	Результат=Запрос.Выполнить().Выбрать();
	
	Пока Результат.Следующий() Цикл
		
		ТекстПроверки=ТекстПроверки+Символы.ПС+СтрШаблон(Нстр("ru = 'Права доступа к данным по виду отчета %1 настраиваются в группе доступа %2'"),Результат.ВидОтчета,Результат.ГруппаДоступа);
	
	КонецЦикла;
	
	Возврат Сред(ТекстПроверки,2);
	
КонецФункции // ПроверитьВхождениеВГруппу() 