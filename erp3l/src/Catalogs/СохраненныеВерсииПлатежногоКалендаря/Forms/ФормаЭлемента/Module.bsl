
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
		
		Справочники.СохраненныеВерсииПлатежногоКалендаря.УстановитьНаименованиеАвтоматически(Объект);
		
	КонецЕсли;
	
	Если Параметры.ЗначенияЗаполнения.Свойство("СписокОпераций") Тогда
		АдресСпискаОпераций = Параметры.ЗначенияЗаполнения.СписокОпераций;
	КонецЕсли;
	
КонецПроцедуры

&НаСервере
Процедура ПередЗаписьюНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	
	Если ЭтоАдресВременногоХранилища(АдресСпискаОпераций) Тогда
		ТекущийОбъект.СписокОпераций.Очистить();
		СохраненныеОперации = ПолучитьИзВременногоХранилища(АдресСпискаОпераций);
		
		Для Каждого ТекСтр Из СохраненныеОперации Цикл
			
			НоваяСтрока = ТекущийОбъект.СписокОпераций.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ТекСтр);
			НоваяСтрока.УточнениеОперации = Новый ХранилищеЗначения(ТекСтр.УточнениеОперации);
		КонецЦикла;
		
	КонецЕсли;

	
КонецПроцедуры
