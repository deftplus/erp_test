
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("Отбор", ) И Параметры.Отбор.Свойство("Владелец", ) Тогда
		Если Не ЗначениеЗаполнено(Объект.ссылка) Тогда
			Объект.Владелец = Параметры.Отбор.Владелец;
			Элементы.Владелец.Доступность = Ложь;	
		КонецЕсли;
	КонецЕсли;
	Если Параметры.Свойство("Отбор", ) И Параметры.Отбор.Свойство("Номенклатура", ) Тогда
		Если Не ЗначениеЗаполнено(Объект.ссылка) Тогда
			Объект.Номенклатура = Параметры.Отбор.Номенклатура;
			Объект.Наименование = СокрЛП(Объект.Номенклатура);
			Элементы.Номенклатура.Доступность = Ложь;	
		КонецЕсли;
	КонецЕсли;
	Если Параметры.Свойство("ПараметрыВыбора", ) И Параметры.ПараметрыВыбора.Свойство("Владелец", ) Тогда
		Если Не ЗначениеЗаполнено(Объект.ссылка) Тогда
			Объект.Владелец = Параметры.ПараметрыВыбора.Владелец;
			Элементы.Владелец.Доступность = Ложь;	
		КонецЕсли;
	КонецЕсли;
	Если Параметры.Свойство("ПараметрыВыбора", ) И Параметры.ПараметрыВыбора.Свойство("Номенклатура", ) Тогда
		Если Не ЗначениеЗаполнено(Объект.ссылка) Тогда
			Объект.Номенклатура = Параметры.ПараметрыВыбора.Номенклатура;
			Объект.Наименование = СокрЛП(Объект.Номенклатура);
			Элементы.Номенклатура.Доступность = Ложь;	
		КонецЕсли;
	КонецЕсли;		
КонецПроцедуры
