Перем РезультатЗапроса Экспорт;
Перем мМодифицированность Экспорт;

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	// Администратором по умолчанию для новых источников является текущий пользователь.
	Если Ссылка.Пустая() Тогда
		Администратор = ОбщегоНазначенияУХ.ПолучитьЗначениеПеременной("глТекущийПользователь");
		ИсточникДанныхСКД = 1;
		ИспользоватьКакПоказатель = Ложь;
	КонецЕсли;
		
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;	
	ДатаИзменения = ТекущаяДата();
	
КонецПроцедуры

мМодифицированность=Ложь;
