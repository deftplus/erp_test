#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ПриЗаписи(Отказ)
	
	// Обработка пометки удаления группы аналитик
	СверкаВГОУХ.ОбработатьПометкуНаУдалениеПодчиненногоОбъекта(ГруппаРаскрытия, ПометкаУдаления);
	
	// Обработка пометки удаления источников данных
	СверкаВГОУХ.ОбработатьПометкуНаУдалениеПодчиненногоОбъекта(ИсточникДанныхФСДЗакупки, ПометкаУдаления);
	СверкаВГОУХ.ОбработатьПометкуНаУдалениеПодчиненногоОбъекта(ИсточникДанныхФСДСебестоимостьПродаж, ПометкаУдаления);
	Для Каждого ИД_ Из ИсточникиДанных Цикл
		СверкаВГОУХ.ОбработатьПометкуНаУдалениеПодчиненногоОбъекта(ИД_.ИсточникДанных, ПометкаУдаления);
	КонецЦикла;
	
	ОбновитьПовторноИспользуемыеЗначения();
	
КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, ТекстЗаполнения, СтандартнаяОбработка)
	
	Если ДанныеЗаполнения <> Неопределено Тогда
		Если ДанныеЗаполнения.Свойство("ЗаполнитьРазделВГО") Тогда
			Справочники.РазделыСверкиВГО.ЗаполнитьРазделВГО(ЭтотОбъект, ДанныеЗаполнения.ЗаполнитьРазделВГО);		
		КонецЕсли;
	КонецЕсли;
			
КонецПроцедуры

#КонецЕсли
