
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
		
	КэшируемыеЗначения = новый Структура;
	КэшируемыеЗначения.Вставить("Подсказки", Перечисления.ВидыОтношенийКГруппе.ПодсказкаПоВидам());
	
	УправлениеФормой(ЭтотОбъект);
	
КонецПроцедуры

&НаКлиенте
Процедура МетодКонсолидацииПриИзменении(Элемент)
	УправлениеФормой(ЭтотОбъект);
КонецПроцедуры

&НаКлиентеНаСервереБезКонтекста
Процедура УправлениеФормой(Форма)

	Элементы = Форма.Элементы;
	Объект = Форма.Объект;
	
	Элементы.ВидОтношенияКГруппе.Подсказка = Форма.КэшируемыеЗначения.Подсказки.Получить(Объект.ВидОтношенияКГруппе);

КонецПроцедуры
