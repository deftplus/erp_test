
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	СчетаБД = ПолучитьСчетаБД(ПараметрКоманды);

	Для каждого СчетБД Из СчетаБД Цикл
		ОткрытьФорму("Справочник.СчетаБД.ФормаОбъекта", Новый Структура("Ключ", СчетБД), ПараметрыВыполненияКоманды.Источник, Истина);
	КонецЦикла;
			
КонецПроцедуры

&НаСервере
Функция ПолучитьСчетаБД(СчетаПлановСчетов)
	
	Запрос = Новый Запрос(
	"ВЫБРАТЬ
	|	СчетаБД.Ссылка
	|ИЗ
	|	Справочник.СчетаБД КАК СчетаБД
	|ГДЕ
	|	СчетаБД.СчетСсылка В(&СчетаПлановСчетов)");
	Запрос.УстановитьПараметр("СчетаПлановСчетов", СчетаПлановСчетов);
	Возврат Запрос.Выполнить().Выгрузить().ВыгрузитьКолонку("Ссылка");
	
КонецФункции

