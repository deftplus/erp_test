#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ПриЗаписи(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	// Установим значение кеш-пулинга в мастер-счете
	Если МастерСчет.ИнструментРазмещенияСвободныхОстатков <> Перечисления.ИнструментыРазмещенияОстатков.КэшПулинг Тогда
		МастерСчетОбъект = МастерСчет.ПолучитьОбъект();
		МастерСчетОбъект.ИнструментРазмещенияСвободныхОстатков = Перечисления.ИнструментыРазмещенияОстатков.КэшПулинг;
		МастерСчетОбъект.Записать();
	Иначе
		// Не изменяем мастер-счет.
	КонецЕсли;
	Если МастерСчет.ПулЛиквидности <> Ссылка Тогда
		МастерСчетОбъект = МастерСчет.ПолучитьОбъект();
		МастерСчетОбъект.ПулЛиквидности = Ссылка;
		МастерСчетОбъект.Записать();
	Иначе
		// Не изменяем мастер-счет.
	КонецЕсли;
КонецПроцедуры

#КонецЕсли
