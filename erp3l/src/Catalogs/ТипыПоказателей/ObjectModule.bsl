// Записывает зависимые показатели, изменяя их реквизиты согласно текущему типу.
Процедура ЗаписатьДанныеПоказателейПоТипу()
	МассивПоказателей = Справочники.ТипыПоказателей.ПолучитьМассивПоказателейСТипом(Ссылка);
	Для Каждого ТекМассивПоказателей Из МассивПоказателей Цикл
		Справочники.ТипыПоказателей.ПеренестиДанныеТипаВПоказатель(ТекМассивПоказателей, Ссылка);
	КонецЦикла;
КонецПроцедуры

Процедура ПриЗаписи(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	ЗаписатьДанныеПоказателейПоТипу();
КонецПроцедуры
