
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ОтборСписка = СписокЗатронутых.Отбор.Элементы;
	ПолеКомпоновкиТипПоказателя = Новый ПолеКомпоновкиДанных("ТипПоказателя");
	ЭлементОтбораСписка = Неопределено;
	Для Каждого ТекОтборСписка Из ОтборСписка Цикл
		Если ТекОтборСписка.ЛевоеЗначение = ПолеКомпоновкиТипПоказателя Тогда
			ЭлементОтбораСписка = ТекОтборСписка;
		Иначе
			Продолжить;			// Выполняем поиск далее.
		КонецЕсли;
	КонецЦикла;
	Если ЭлементОтбораСписка = Неопределено Тогда
		ЭлементОтбораСписка = ОтборСписка.Добавить(Тип("ЭлементОтбораКомпоновкиДанных"));
	Иначе
		// Уже есть такой элемент.
	КонецЕсли;
	ЭлементОтбораСписка.ЛевоеЗначение		 = ПолеКомпоновкиТипПоказателя;
	ЭлементОтбораСписка.ВидСравнения		 = ВидСравненияКомпоновкиДанных.Равно;
	ЭлементОтбораСписка.Использование		 = Истина;
	Если ЗначениеЗаполнено(Объект.Ссылка) Тогда
		ЭлементОтбораСписка.ПравоеЗначение	 = Объект.Ссылка;
	Иначе
		ЭлементОтбораСписка.ПравоеЗначение	 = Неопределено;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
	Оповестить("ОбновитьТипПоказателя");
КонецПроцедуры
