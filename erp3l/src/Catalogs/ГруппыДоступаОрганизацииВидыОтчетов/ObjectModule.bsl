
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
Процедура ПриЗаписи(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	НастройкаПравДоступаУХ.ОбновитьДляГруппы("Организация", Ссылка);
	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	ТекстПроверки=Справочники.ГруппыДоступаОрганизацииВидыОтчетов.ПроверитьВхождениеВГруппу(Ссылка,Организации.Выгрузить());
	
	Если Не ПустаяСтрока(ТекстПроверки) Тогда
		
		ОбщегоНазначенияУХ.СообщитьОбОшибке(ТекстПроверки,Отказ,,СтатусСообщения.Важное);
		
	КонецЕсли;
	
	//Если ВключаяПодчиненные Тогда
	//	
	//	Запрос=Новый Запрос;
	//	Запрос.Текст="ВЫБРАТЬ
	//	|	Организации.Ссылка КАК Организация
	//	|ИЗ
	//	|	Справочник.Организации КАК Организации
	//	|ГДЕ
	//	|	Организации.Ссылка В ИЕРАРХИИ(&ОрганизацииГруппы)";
	//	
	//	Запрос.УстановитьПараметр("ОрганизацииГруппы",ГруппыОрганизации.Выгрузить().ВыгрузитьКолонку("Организация"));
	//	
	//	Организации.Загрузить(Запрос.Выполнить().Выгрузить());
	//	
	//Иначе
	//	
	//	Организации.Загрузить(ГруппыОрганизации.Выгрузить());
	//	
	//КонецЕсли;
	
КонецПроцедуры

#КонецЕсли
