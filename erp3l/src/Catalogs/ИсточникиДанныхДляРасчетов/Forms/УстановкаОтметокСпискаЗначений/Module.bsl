
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	РедактируемыйСписок=Параметры.РедактируемыйСписок;
	
	Для Каждого Элемент ИЗ Параметры.ТекущийСписок Цикл
		
		ТекЭлемент=РедактируемыйСписок.НайтиПоЗначению(Элемент.Значение);
		ТекЭлемент.Пометка=Истина;
		
	КонецЦикла;
	
	Заголовок=Параметры.ПолеБД;
	 
КонецПроцедуры

&НаКлиенте
Процедура ОК(Команда)
	
	СписокВозврат=Новый СписокЗначений;
	
	Для Каждого Элемент ИЗ РедактируемыйСписок Цикл
		
		Если Элемент.Пометка Тогда
			
			СписокВозврат.Добавить(Элемент.Значение);
			
		КонецЕсли;
		
	КонецЦикла;
	
	ОповеститьОВыборе(СписокВозврат);
	
КонецПроцедуры
