
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
	
	Если ЭтоГруппа Тогда
		Возврат;
	КонецЕсли;
	
	// Корректно проверить настройку диапазонов можно только в том случае, если границы заданы в виде числовых констант.
	Если ТипЗнч(Состояние_ЛевыйДиапазон) = тип("Число") И ТипЗнч(Состояние_ПравыйДиапазон) = Тип("Число") Тогда
		Отказ = ТрактовкаПоложительногоОтклонения = (Состояние_ЛевыйДиапазон >= Состояние_ПравыйДиапазон);
	КонецЕсли;
	
	Если Отказ Тогда
		СообщениеПользователю = Новый СообщениеПользователю;
		СообщениеПользователю.Текст = НСтр("ru = 'Неверно заданы границы для определения состояния'");
		СообщениеПользователю.Сообщить();
		ПроверяемыеРеквизиты.Очистить();
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)
	
	Если ЭтоГруппа Тогда
		Возврат;
	КонецЕсли;
	
	Если Ссылка.Пустая() Тогда
		ТрактованиеПоложительногоОтклонения = Истина;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПередЗаписью(Отказ)
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;	
	Если НЕ ЭтоГруппа Тогда
		СхемаКомпоновкиДляРасшифровки = Новый ХранилищеЗначения(ТиповыеОтчетыУХ.ПолучитьСхемуКомпоновкиДанныхДляРасшифровкиПоказателяМонитора(ЭтотОбъект));
	КонецЕсли;
	
КонецПроцедуры
