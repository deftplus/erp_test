
// Возвращает массив форм, доступных для выбора для выбора в текущей схеме доступности реквизитов.
Функция ПолучитьМассивДоступныхФорм() Экспорт
	РезультатФункции = Новый Массив;
	Если ЗначениеЗаполнено(СогласуемыйОбъект) Тогда
		Если ТипЗнч(СогласуемыйОбъект) = Тип("СправочникСсылка.СправочникиБД") Тогда
			РезультатФункции = СогласуемыйОбъект.ДоступныеФормыСхемРеквизитов.ВыгрузитьКолонку("ИмяДоступнойФормы");	
		ИначеЕсли ТипЗнч(СогласуемыйОбъект) = Тип("СправочникСсылка.ДокументыБД") Тогда
			РезультатФункции = СогласуемыйОбъект.ДоступныеФормыСхемРеквизитов.ВыгрузитьКолонку("ИмяДоступнойФормы");	
		Иначе
			РезультатФункции = Новый Массив;
		КонецЕсли;
	Иначе
		РезультатФункции = Новый Массив;
	КонецЕсли;
	Возврат РезультатФункции;
КонецФункции		// ПолучитьМассивДоступныхФорм()