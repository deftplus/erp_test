
&НаКлиенте
Процедура ПараметрыРасчетаТипЗначенияПараметраНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка=Ложь;
	ОткрытьФорму("Справочник.ФормулаРасчета.Форма.ФормаВыбораТипаЗначения",Новый Структура("ПараметрФормулы,СтрокаТипов",Истина,Элементы.ПараметрыРасчета.ТекущиеДанные.ТипЗначенияПараметра));
	
КонецПроцедуры

&НаКлиенте
Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия="ИзмененТипЗначенияПараметраФормулы" Тогда
		
		Элементы.ПараметрыРасчета.ТекущиеДанные.ТипЗначенияПараметра=Параметр;
		
	КонецЕсли;
	
КонецПроцедуры


&НаКлиенте
Процедура ПараметрыРасчетаПередОкончаниемРедактирования(Элемент, НоваяСтрока, ОтменаРедактирования, Отказ)
	
	ПредставлениеПараметра=Элементы.ПараметрыРасчета.ТекущиеДанные.ПредставлениеПараметра;
	
	Если Не (ЗначениеЗаполнено(ПредставлениеПараметра) ИЛИ ОтменаРедактирования) Тогда
		
		ПоказатьПредупреждение(,"Не заполнено имя параметра");
		Отказ=Истина;
		Возврат;
		
	КонецЕсли;
	
	Элементы.ПараметрыРасчета.ТекущиеДанные.ИмяПараметра=ОбщегоНазначенияКлиентСерверУХ.ПолучитьКлючПоНаименованию(ПредставлениеПараметра);
	
КонецПроцедуры


&НаКлиенте
Процедура ПараметрыРасчетаЗначениеПараметраНачалоВыбора(Элемент, ДанныеВыбора, СтандартнаяОбработка)
	
	СтандартнаяОбработка=Ложь;
	
	
КонецПроцедуры

