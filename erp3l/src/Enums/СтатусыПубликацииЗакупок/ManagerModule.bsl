#Если НаСервере Тогда
Функция ПолучитьСтатусыЗакрытыхПубликаций() Экспорт
	мСтатусыЗакрытыхПубликаций = Новый Массив;
	мСтатусыЗакрытыхПубликаций.Добавить(Перечисления.СтатусыПубликацииЗакупок.ЗакрытаОтказ);
	мСтатусыЗакрытыхПубликаций.Добавить(Перечисления.СтатусыПубликацииЗакупок.ЗакрытаУспешно);
	мСтатусыЗакрытыхПубликаций.Добавить(Перечисления.СтатусыПубликацииЗакупок.Отменена);
	Возврат мСтатусыЗакрытыхПубликаций;
КонецФункции

Функция ЭтоСтатусЗакрытойПубликации(СтатусДляПроверки) Экспорт
	мСтатусыЗакрытыхПубликаций = ПолучитьСтатусыЗакрытыхПубликаций();
	Возврат мСтатусыЗакрытыхПубликаций.Найти(СтатусДляПроверки) <> Неопределено;
КонецФункции

#КонецЕсли
