
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	ПараметрыФормы = Новый Структура;
	Если ЭтоУХ() Тогда
		ПутьКФорме = "Документ.ОперативныйПлан.ФормаСписка";
		ПараметрыФормы.Вставить("ПланированиеЗакупок", Истина);
		ПараметрыФормы.Вставить(
			"Отбор",
			Новый Структура("ВидБюджета", 
				ПредопределенноеЗначение(
					"Перечисление.ПредназначенияЭлементовСтруктурыОтчета.БюджетДвиженияРесурсов")));
	Иначе
		ПутьКФорме = "Документ.ПланЗакупок.ФормаСписка";
		ПараметрыФормы.Вставить("ЭтоПланЦентрализованныхЗакупок", Истина);
	КонецЕсли;
	ОткрытьФорму(
		ПутьКФорме,
		ПараметрыФормы,
		ПараметрыВыполненияКоманды.Источник,
		Истина,
		ПараметрыВыполненияКоманды.Окно,
		ПараметрыВыполненияКоманды.НавигационнаяСсылка);
КонецПроцедуры

&НаСервере
Функция ЭтоУХ()
	Возврат НЕ ОбщегоНазначения.ПодсистемаСуществует("ПереопределяемыеОбъектыУП"); 
КонецФункции
