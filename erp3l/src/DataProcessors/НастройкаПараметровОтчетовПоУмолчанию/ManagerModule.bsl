#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

Функция ВернутьПредставлениеСпособаФормированияОтчета(СтрокаТаблицы) Экспорт
	
	ПредставлениеОтчета = Строка(СтрокаТаблицы.СпособФормированияОтчета);
	Если СтрокаТаблицы.СпособФормированияОтчета = Перечисления.СпособыФормированияОтчетов.АвтоматическиСвернувПоПериоду Тогда
		ПредставлениеОтчета = СтрШаблон(Нстр("ru = '%1
							  |Периодичность: %2'"), ПредставлениеОтчета, Строка(СтрокаТаблицы.Периодичность));
	ИначеЕсли СтрокаТаблицы.СпособФормированияОтчета = Перечисления.СпособыФормированияОтчетов.Импорт Тогда
		ПредставлениеОтчета = СтрШаблон(Нстр("ru = '%1
							  |Внешняя ИБ: %2'"), ПредставлениеОтчета, 
							  ?(ЗначениеЗаполнено(СтрокаТаблицы.ВнешняяИнформационнаяБаза), Строка(СтрокаТаблицы.ВнешняяИнформационнаяБаза) + "
							  |(" + СтрокаТаблицы.ВнешняяИнформационнаяБаза.ТипБД + ")"
							  , "не указана"));
	КонецЕсли;
	
	Возврат ПредставлениеОтчета;
	
КонецФункции

Функция ВернутьТаблицуВалютОтчетности(ВерсияОрганизационнойСтруктуры) Экспорт
	Возврат РегистрыСведений.ВалютыОтчетности.ВернутьТаблицуВалютОтчетности(ВерсияОрганизационнойСтруктуры);			
КонецФункции

#КонецЕсли