
&НаСервереБезКонтекста
Процедура Команда1НаСервере(_С)        
	
    Если ЗначениеЗаполнено(_С)	тогда
   		инт_JSONОбмен.РаспознатьИмяОбъектаСообщенияОбмена(_С);
	иначе
		инт_JSONОбмен.РаспознатьИмяОбъектаСообщенияОбмена(неопределено);		
		
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура Команда1(Команда) 
	Если ЗначениеЗаполнено(Объект.Сообщение) тогда
		Команда1НаСервере(Объект.Сообщение);                              
	иначе
		Команда1НаСервере(неопределено);                              
	КонецЕсли;	
	
КонецПроцедуры
