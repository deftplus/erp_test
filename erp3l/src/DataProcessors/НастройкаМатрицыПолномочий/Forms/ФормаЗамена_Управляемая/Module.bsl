
&НаКлиенте
Процедура ОК(Команда)
	
	СтруктураОтвета = Новый Структура("ИсходныйПользователь, ЗаменяющийПользователь, ЗаменятьИсполняющего, ЗаменятьОтветственного"
										, ИсходныйПользователь
										, ЗаменяющийПользователь
										, ЗаменятьИсполняющего
										, ЗаменятьОтветственного);
	
	Закрыть(Новый ФиксированнаяСтруктура(СтруктураОтвета));
	
КонецПроцедуры
