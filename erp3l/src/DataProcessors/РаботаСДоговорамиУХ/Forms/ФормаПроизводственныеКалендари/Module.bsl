#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	ПроизводственныеКалендари.Загрузить(Параметры.Объект.ПроизводственныеКалендари.Выгрузить());
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовШапкиФормы

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура КомандаОК(Команда)
	
	Календари = Новый Массив;
	Для каждого ТекСтрока из ПроизводственныеКалендари Цикл
		Календари.Добавить(ТекСтрока.ПроизводственныйКалендарь);
	КонецЦикла;
	
	Закрыть(Календари);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

#КонецОбласти