
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	ИмяДокумента = ИмяДокумента(ПараметрКоманды);
	ИмяФормыВерсий = СтрШаблон("Документ.%1.ФормаСписка", ИмяДокумента);
	

	СтруктураОтбора = Новый Структура("ДоговорКонтрагента", ПараметрКоманды);
	ОткрытьФорму(ИмяФормыВерсий, Новый Структура("Отбор", СтруктураОтбора));
	
КонецПроцедуры

&НаСервере
Функция ИмяДокумента(Договор)
	ВидДоговора = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Договор, "ВидДоговораУХ");
	Возврат УправлениеДоговорамиУХКлиентСерверПовтИсп.ПолучитьИмяДокументаПоВидуДоговора(ВидДоговора);
КонецФункции

