
&НаКлиенте
Процедура Выбрать(Команда)
	
	Закрыть(Новый Структура("ВыбраннаяКартинка",Элементы.СписокДоступныхКартинок.ТекущиеДанные.Иконка));
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Нстр 				= СписокДоступныхКартинок.Добавить();
	Нстр.ИмяКартинки    = "Без картинки";
	
	Для Каждого СтрКартинка Из Метаданные.ОбщиеКартинки Цикл
		
		Нстр 				= СписокДоступныхКартинок.Добавить();
		Нстр.ИмяКартинки    = СтрКартинка.Имя;
		Иконка         		= БиблиотекаКартинок[СтрКартинка.Имя];
		Нстр.Иконка         = БиблиотекаКартинок[СтрКартинка.Имя];
			
	КонецЦикла;	
		
КонецПроцедуры

&НаКлиенте
Процедура СписокДоступныхКартинокВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	Выбрать(Неопределено);
	
КонецПроцедуры
