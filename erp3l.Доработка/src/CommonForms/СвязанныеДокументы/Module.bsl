&Вместо("ПриСозданииНаСервере")
	Процедура ллл_ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	ОпределитьНастройки();
	
	Параметры.Свойство("ОбъектОтбора", ОсновнойОбъект);
	ИсходныйОбъект = ОсновнойОбъект;
	
	//Если не ЗначениеЗаполнено(ОсновнойОбъект) и типЗнч(Параметры.ОбъектОтбора)=тип("ДокументСсылка.ллл_ПредварительныйЗаказЗаводуИзготовителю") тогда
	//	
	//			//Отказ=истина;
	//			//ОткрытьФорму("ОбщаяФорма.ллл_СвязанныеДокументы",Параметры);
	//			
	//					
	//иначе	
	
	
	
	//Если ЗначениеЗаполнено(ОсновнойОбъект) и не типЗнч(ОсновнойОбъект)=тип("ДокументСсылка.ллл_ПредварительныйЗаказЗаводуИзготовителю") Тогда
	//	ОбновитьДеревоСтруктурыПодчиненности();
	//КонецЕсли;
	
	
	//КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ллл_ПриОткрытииПеред(Отказ)
	Если типЗнч(ОсновнойОбъект)=тип("ДокументСсылка.ллл_ПредварительныйЗаказЗаводуИзготовителю") или 
		типЗнч(ОсновнойОбъект)=тип("ДокументСсылка.ЗаказПоставщику") или 
		типЗнч(ОсновнойОбъект)=тип("ДокументСсылка.ЗаявкаНаРасходованиеДенежныхСредств") или
		типЗнч(ОсновнойОбъект)=тип("ДокументСсылка.СписаниеБезналичныхДенежныхСредств") или  
		типЗнч(ОсновнойОбъект)=тип("ДокументСсылка.ПриобретениеТоваровУслуг")
		
		тогда
		      Отказ=истина;
		      	ОткрытьФорму("ОбщаяФорма.ллл_СвязанныеДокументы",Новый Структура("ОбъектОтбора", ОсновнойОбъект));

	КонецЕсли;
		
КонецПроцедуры


